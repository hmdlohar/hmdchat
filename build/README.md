Build assets for packaging.

Add icons here before running packaged builds:

- `build/icons/256x256.png` or other Linux PNG sizes
- `build/icons/icon.ico` for Windows installer/app icon

Electron builder reads from `build/icons`.
